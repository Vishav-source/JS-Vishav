const id=12345
let accountpassword="vishav@8458"
// we don't use var because of scope thing, we only use let variable
accounttype="sbi_account"
accountpassword="baigra@8458"

console.table([id,accountpassword,accounttype])